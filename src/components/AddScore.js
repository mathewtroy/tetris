import React, { Component } from 'react';

class AddScore extends Component {
  // Method to update score
  updateScore = (linesCleared) => {
    // Implementation...
  }

  render() {
    return null; // Might not render anything itself, but could if needed
  }
}

export default AddScore;
